1. install UV for my mac to manage packages that we need it from our project
2. install langchain with UV ( uv add langchain) & ( `langchain-openai`)
3. installing python-dotenv. for handle env & uv add isort black for code pritter
4. *isort* is a *Python* utility / library to sort imports alphabetically, and automatically separated into sections and by type.
5. black: an **uncompromising, opinionated Python code formatter** that automatically restyles an entire codebase to a consistent
6.
